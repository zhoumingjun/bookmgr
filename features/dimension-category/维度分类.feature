# language: zh-CN
功能: 维度分类管理

  作为一个 超级管理员或普通管理员
  我希望管理五大适应维度及二级子分类
  以便为绘本资源提供科学的分类体系

  背景:
    假设 超级管理员"王老师"已登录系统
    假设 系统当前无任何维度分类

  场景: 超级管理员创建一级维度
    当 王老师 访问维度管理页面
    而且 王老师 点击"添加维度"按钮
    而且 王老师 填写维度信息：
      | 字段 | 值 |
      | 维度名称 | 身心准备 |
      | Slug | physical |
      | 描述 | 身体动作、感知觉、精细动作等身体层面的发展 |
    而且 王老师 点击"保存"按钮
    那么 系统返回新维度信息
    而且 维度名称为"身心准备"
    而且 维度 Slug 为"physical"

  场景: 超级管理员创建二级子分类
    假设 王老师 已创建一级维度"身心准备"
    当 王老师 在维度"身心准备"下点击"添加子分类"
    而且 王老师 填写子分类信息：
      | 字段 | 值 |
      | 子分类名称 | 校园作息适应 |
      | Slug | school-routine |
    而且 王老师 点击"保存"按钮
    那么 系统返回子分类信息
    而且 子分类挂载在一级维度"身心准备"下

  场景: 超级管理员创建多个二级子分类
    假设 王老师 已创建一级维度"身心准备"
    当 王老师 在维度"身心准备"下添加以下子分类：
      | 子分类名称 | Slug |
      | 校园作息适应 | school-routine |
      | 身体动作发展 | physical-development |
      | 校园规则感知 | school-rules |
    而且 王老师 点击"保存"按钮
    那么 一级维度"身心准备"下有三个子分类

  场景: 超级管理员编辑维度
    假设 王老师 已创建维度"身心准备"
    当 王老师 编辑维度"身心准备"
    而且 王老师 将维度名称修改为"身心发展"
    而且 王老师 点击"保存"按钮
    那么 维度名称更新为"身心发展"

  场景: 超级管理员删除空维度
    假设 王老师 已创建维度"测试维度"（无子分类、无关联绘本）
    当 王老师 删除维度"测试维度"
    而且 王老师 在确认对话框中点击"确认"
    那么 系统返回成功
    而且 维度"测试维度"不再出现在维度列表中

  场景: 超级管理员删除有绘本关联的维度
    假设 王老师 已创建维度"身心准备"，且有关联绘本
    当 王老师 尝试删除维度"身心准备"
    那么 系统返回 409 Conflict 错误
    而且 错误信息包含"该维度下有关联绘本，无法删除"
    而且 维度"身心准备"保留在系统中

  场景: 普通管理员创建维度
    假设 普通管理员"张管理员"已存在
    假设 张管理员 已登录系统
    当 张管理员 访问维度管理页面
    而且 张管理员 创建一级维度"学习准备"
    那么 系统返回新维度信息

  场景: 普通管理员无权创建一级维度（限制场景）
    假设 普通管理员"张管理员"已存在（产品决策：普通管理员可创建维度）
    # （本场景备用，如产品决策变更可启用）
    假设 张管理员 已登录系统
    当 张管理员 尝试创建一级维度
    那么 系统返回 403 无权限错误

  场景: 获取维度树形列表
    假设 王老师 已创建以下维度结构：
      | 维度名称 | 子分类 |
      | 身心准备 | 校园作息适应、身体动作发展 |
      | 生活准备 | 穿衣、洗漱 |
    当 王老师 发送 GET 请求到 /api/v1/dimensions
    那么 响应返回树形结构，包含两个一级维度
    而且 每个一级维度包含其子分类列表

  场景: 按父维度筛选子分类
    假设 王老师 已创建维度结构
    当 王老师 发送 GET 请求到 /api/v1/dimensions?parent=physical
    那么 响应仅返回"身心准备"下的子分类

  场景: 获取单个维度详情
    假设 王老师 已创建维度"身心准备"
    当 王老师 发送 GET 请求到 /api/v1/dimensions/physical
    那么 响应返回维度完整信息，包括名称、Slug、描述、子分类列表

  场景: 创建重复 Slug 的维度
    假设 王老师 已创建维度 Slug 为"physical"
    当 王老师 尝试创建新维度 Slug 也为"physical"
    那么 系统返回 409 Conflict 错误
    而且 错误信息包含"Slug 已存在"

  场景: 教师用户无权访问维度管理
    假设 教师用户"李老师"已存在
    假设 李老师 已登录系统
    当 李老师 尝试访问维度管理页面
    那么 系统返回 403 无权限错误

  @api-only
  场景: API 创建维度
    假设 王老师 已登录系统
    当 王老师 发送 POST 请求到 /api/v1/dimensions 包含：
      | 字段 | 值 |
      | name | 情绪准备 |
      | slug | emotional |
    那么 响应状态码为 200
    而且 响应包含 id 和 slug

  @api-only
  场景: API 更新维度
    假设 王老师 已创建维度"情绪准备"
    假设 王老师 已登录系统
    当 王老师 发送 PUT 请求到 /api/v1/dimensions/{id} 包含：
      | 字段 | 值 |
      | name | 情绪行为 |
    那么 响应状态码为 200
    而且 维度名称为"情绪行为"

  @api-only
  场景: API 删除维度（无关联）
    假设 王老师 已创建空维度"测试"
    假设 王老师 已登录系统
    当 王老师 发送 DELETE 请求到 /api/v1/dimensions/{id}
    那么 响应状态码为 204

  @api-only
  场景: API 获取子分类列表
    假设 王老师 已创建维度及其子分类
    假设 王老师 已登录系统
    当 王老师 发送 GET 请求到 /api/v1/dimensions/physical/subcategories
    那么 响应返回子分类列表

# language: zh-CN
功能: 角色权限
  作为系统
  我需要根据用户角色控制访问权限
  以确保系统安全

  @e2e-only
  场景: 普通用户无法访问管理页面
    假如 我已登录为普通用户
    当 我尝试访问 "/admin/users"
    那么 我应该被重定向到图书浏览页面

  @e2e-only
  场景: 管理员可以访问管理页面
    假如 我已登录为管理员
    当 我尝试访问 "/admin/users"
    那么 我应该看到用户列表表格

  @api-only
  场景大纲: API 普通用户无法访问管理接口
    假如 我已登录为普通用户
    当 我发送 <方法> 请求到 "<路径>"
    那么 响应状态码应该是 403

    例子:
      | 方法   | 路径              |
      | GET    | /api/v1/users     |
      | DELETE | /api/v1/users/00000000-0000-0000-0000-000000000000 |

  @api-only
  场景: 未认证用户无法访问受保护接口
    当 我不带认证发送 GET 请求到 "/api/v1/books"
    那么 响应状态码应该是 401

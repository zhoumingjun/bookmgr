# language: zh-CN
功能: 前端特教适配

  作为一个 特教教师或家长用户
  我希望系统界面适配特教人群的视觉和操作习惯
  以便高效使用绘本资源管理平台

  背景:
    假设 系统已实现基础的用户认证和角色权限
    假设 教师用户"李老师"已登录系统
    假设 家长用户"王家长"已登录系统

  场景: 教师用户登录后跳转到教师端
    假设 李老师 的角色为"教师用户"
    当 李老师 登录系统
    那么 系统跳转到 /teacher/browse 页面
    而且 左侧导航显示教师端专属菜单

  场景: 家长用户登录后跳转到家长端
    假设 王家长 的角色为"家长用户"
    当 王家长 登录系统
    那么 系统跳转到 /parent/browse 页面
    而且 左侧导航显示家长端专属菜单

  场景: 管理员登录后跳转到管理后台
    假设 超级管理员"王老师"已登录系统
    当 王老师 登录系统
    那么 系统跳转到 /admin/dashboard
    而且 左侧导航显示管理后台菜单

  场景: 教师端菜单项正确
    假设 李老师 已登录系统并跳转到教师端
    那么 左侧导航显示"绘本浏览"
    而且 左侧导航显示"我的收藏"
    而且 左侧导航显示"提交审核"
    而且 左侧导航显示"我的反馈"

  场景: 家长端菜单项正确
    假设 王家长 已登录系统并跳转到家长端
    那么 左侧导航显示"绘本浏览"
    而且 左侧导航显示"我的收藏"
    而且 左侧导航显示"阅读记录"
    而且 左侧导航显示"我的反馈"

  场景: 教师端不显示管理菜单
    假设 李老师 已登录系统并跳转到教师端
    那么 左侧导航不显示"用户管理"菜单
    而且 左侧导航不显示"绘本上传"菜单
    而且 左侧导航不显示"审核管理"菜单

  场景: 家长端不显示管理菜单
    假设 王家长 已登录系统并跳转到家长端
    那么 左侧导航不显示"用户管理"菜单
    而且 左侧导航不显示"绘本上传"菜单
    而且 左侧导航不显示"审核管理"菜单

  场景: 字体缩放切换到特大（1.5倍）
    假设 李老师 访问任意页面
    当 李老师 在页面右上角点击"字号"设置
    而且 李老师 选择"特大（1.5x）"
    那么 全站字体从 16px 变为 24px
    而且 所有按钮和标签文字同步放大

  场景: 字体缩放切换到超特大（2倍）
    假设 李老师 点击"字号"设置
    而且 李老师 选择"超特大（2x）"
    那么 全站字体从 16px 变为 32px
    而且 按钮的 min-height 从 32px 变为 64px

  场景: 字号设置刷新后保持
    假设 李老师 设置字号为"特大（1.5x）"
    当 李老师 刷新页面
    那么 字号仍保持"特大（1.5x）"
    而且 无需重新选择

  场景: 开启高对比模式
    假设 李老师 点击右上角"对比度"开关
    而且 李老师 开启高对比模式
    那么 页面背景变为纯白色（#FFFFFF）
    而且 正文文字变为纯黑色（#000000）
    而且 主要按钮变为高饱和蓝色（#0066CC）

  场景: 关闭高对比模式
    假设 高对比模式已开启
    当 李老师 关闭高对比模式开关
    那么 页面完全恢复 Ant Design 默认配色

  场景: 高对比模式刷新后保持
    假设 李老师 开启高对比模式
    当 李老师 刷新页面
    那么 高对比模式保持开启

  场景: 2倍缩放下按钮触摸目标尺寸增大
    假设 李老师 设置字号为"超特大（2x）"
    当 李老师 查看任意按钮
    那么 按钮的 min-height 不小于 44px（符合 WCAG 触摸友好标准）

  场景: 所有交互元素焦点可见
    假设 李老师 使用 Tab 键导航页面
    当 光标移动到任意按钮或链接
    那么 焦点指示器清晰可见（3px solid #0066cc outline）

  场景: 教师端家长端侧边栏布局一致
    假设 李老师 访问教师端绘本浏览页面
    而且 王家长 访问家长端绘本浏览页面
    那么 两者的侧边栏宽度和布局保持一致
    而且 功能按钮大小一致

  场景: 家长端不显示审核入口
    假设 王家长 已登录家长端
    当 王家长 访问绘本详情页
    那么 详情页不显示"提交审核"按钮

  @e2e-only
  场景: E2E 完整字体和高对比切换流程
    假设 李老师 已登录系统
    当 李老师 访问绘本浏览页面
    而且 李老师 设置字号为"超特大（2x）"
    而且 李老师 开启高对比模式
    而且 李老师 刷新页面
    而且 字号仍为"超特大"
    而且 高对比模式保持开启
    而且 页面布局正常无错位

  @e2e-only
  场景: E2E 教师端家长端路由隔离
    假设 李老师 已登录为教师
    假设 王家长 已登录为家长
    当 李老师 尝试直接访问 /parent/browse
    而且 系统返回 403 或重定向到 /teacher/browse
    当 王家长 尝试直接访问 /teacher/browse
    而且 系统返回 403 或重定向到 /parent/browse

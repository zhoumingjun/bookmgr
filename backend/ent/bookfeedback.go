// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/zhoumingjun/bookmgr/backend/ent/bookfeedback"
)

// BookFeedback is the model entity for the BookFeedback schema.
type BookFeedback struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// BookID holds the value of the "book_id" field.
	BookID uuid.UUID `json:"book_id,omitempty"`
	// FeedbackType holds the value of the "feedback_type" field.
	FeedbackType bookfeedback.FeedbackType `json:"feedback_type,omitempty"`
	// DifficultyRating holds the value of the "difficulty_rating" field.
	DifficultyRating int `json:"difficulty_rating,omitempty"`
	// UseScenario holds the value of the "use_scenario" field.
	UseScenario string `json:"use_scenario,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BookFeedbackQuery when eager-loading is set.
	Edges        BookFeedbackEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BookFeedbackEdges holds the relations/edges for other nodes in the graph.
type BookFeedbackEdges struct {
	// User holds the value of the user edge.
	User []*User `json:"user,omitempty"`
	// Book holds the value of the book edge.
	Book []*Book `json:"book,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading.
func (e BookFeedbackEdges) UserOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// BookOrErr returns the Book value or an error if the edge
// was not loaded in eager-loading.
func (e BookFeedbackEdges) BookOrErr() ([]*Book, error) {
	if e.loadedTypes[1] {
		return e.Book, nil
	}
	return nil, &NotLoadedError{edge: "book"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BookFeedback) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bookfeedback.FieldDifficultyRating:
			values[i] = new(sql.NullInt64)
		case bookfeedback.FieldFeedbackType, bookfeedback.FieldUseScenario:
			values[i] = new(sql.NullString)
		case bookfeedback.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case bookfeedback.FieldID, bookfeedback.FieldUserID, bookfeedback.FieldBookID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BookFeedback fields.
func (_m *BookFeedback) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bookfeedback.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bookfeedback.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		case bookfeedback.FieldBookID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field book_id", values[i])
			} else if value != nil {
				_m.BookID = *value
			}
		case bookfeedback.FieldFeedbackType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field feedback_type", values[i])
			} else if value.Valid {
				_m.FeedbackType = bookfeedback.FeedbackType(value.String)
			}
		case bookfeedback.FieldDifficultyRating:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field difficulty_rating", values[i])
			} else if value.Valid {
				_m.DifficultyRating = int(value.Int64)
			}
		case bookfeedback.FieldUseScenario:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field use_scenario", values[i])
			} else if value.Valid {
				_m.UseScenario = value.String
			}
		case bookfeedback.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BookFeedback.
// This includes values selected through modifiers, order, etc.
func (_m *BookFeedback) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the BookFeedback entity.
func (_m *BookFeedback) QueryUser() *UserQuery {
	return NewBookFeedbackClient(_m.config).QueryUser(_m)
}

// QueryBook queries the "book" edge of the BookFeedback entity.
func (_m *BookFeedback) QueryBook() *BookQuery {
	return NewBookFeedbackClient(_m.config).QueryBook(_m)
}

// Update returns a builder for updating this BookFeedback.
// Note that you need to call BookFeedback.Unwrap() before calling this method if this BookFeedback
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BookFeedback) Update() *BookFeedbackUpdateOne {
	return NewBookFeedbackClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BookFeedback entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BookFeedback) Unwrap() *BookFeedback {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BookFeedback is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BookFeedback) String() string {
	var builder strings.Builder
	builder.WriteString("BookFeedback(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("book_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.BookID))
	builder.WriteString(", ")
	builder.WriteString("feedback_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.FeedbackType))
	builder.WriteString(", ")
	builder.WriteString("difficulty_rating=")
	builder.WriteString(fmt.Sprintf("%v", _m.DifficultyRating))
	builder.WriteString(", ")
	builder.WriteString("use_scenario=")
	builder.WriteString(_m.UseScenario)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BookFeedbacks is a parsable slice of BookFeedback.
type BookFeedbacks []*BookFeedback

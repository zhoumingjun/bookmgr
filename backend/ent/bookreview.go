// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/zhoumingjun/bookmgr/backend/ent/bookreview"
)

// BookReview is the model entity for the BookReview schema.
type BookReview struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BookID holds the value of the "book_id" field.
	BookID uuid.UUID `json:"book_id,omitempty"`
	// ReviewerID holds the value of the "reviewer_id" field.
	ReviewerID uuid.UUID `json:"reviewer_id,omitempty"`
	// Action holds the value of the "action" field.
	Action string `json:"action,omitempty"`
	// StatusFrom holds the value of the "status_from" field.
	StatusFrom string `json:"status_from,omitempty"`
	// StatusTo holds the value of the "status_to" field.
	StatusTo string `json:"status_to,omitempty"`
	// Reason holds the value of the "reason" field.
	Reason string `json:"reason,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BookReviewQuery when eager-loading is set.
	Edges        BookReviewEdges `json:"edges"`
	book_reviews *uuid.UUID
	user_reviews *uuid.UUID
	selectValues sql.SelectValues
}

// BookReviewEdges holds the relations/edges for other nodes in the graph.
type BookReviewEdges struct {
	// Book holds the value of the book edge.
	Book []*Book `json:"book,omitempty"`
	// Reviewer holds the value of the reviewer edge.
	Reviewer []*User `json:"reviewer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// BookOrErr returns the Book value or an error if the edge
// was not loaded in eager-loading.
func (e BookReviewEdges) BookOrErr() ([]*Book, error) {
	if e.loadedTypes[0] {
		return e.Book, nil
	}
	return nil, &NotLoadedError{edge: "book"}
}

// ReviewerOrErr returns the Reviewer value or an error if the edge
// was not loaded in eager-loading.
func (e BookReviewEdges) ReviewerOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.Reviewer, nil
	}
	return nil, &NotLoadedError{edge: "reviewer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BookReview) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bookreview.FieldAction, bookreview.FieldStatusFrom, bookreview.FieldStatusTo, bookreview.FieldReason:
			values[i] = new(sql.NullString)
		case bookreview.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case bookreview.FieldID, bookreview.FieldBookID, bookreview.FieldReviewerID:
			values[i] = new(uuid.UUID)
		case bookreview.ForeignKeys[0]: // book_reviews
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case bookreview.ForeignKeys[1]: // user_reviews
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BookReview fields.
func (_m *BookReview) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bookreview.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bookreview.FieldBookID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field book_id", values[i])
			} else if value != nil {
				_m.BookID = *value
			}
		case bookreview.FieldReviewerID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field reviewer_id", values[i])
			} else if value != nil {
				_m.ReviewerID = *value
			}
		case bookreview.FieldAction:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field action", values[i])
			} else if value.Valid {
				_m.Action = value.String
			}
		case bookreview.FieldStatusFrom:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status_from", values[i])
			} else if value.Valid {
				_m.StatusFrom = value.String
			}
		case bookreview.FieldStatusTo:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status_to", values[i])
			} else if value.Valid {
				_m.StatusTo = value.String
			}
		case bookreview.FieldReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reason", values[i])
			} else if value.Valid {
				_m.Reason = value.String
			}
		case bookreview.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case bookreview.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field book_reviews", values[i])
			} else if value.Valid {
				_m.book_reviews = new(uuid.UUID)
				*_m.book_reviews = *value.S.(*uuid.UUID)
			}
		case bookreview.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_reviews", values[i])
			} else if value.Valid {
				_m.user_reviews = new(uuid.UUID)
				*_m.user_reviews = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BookReview.
// This includes values selected through modifiers, order, etc.
func (_m *BookReview) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBook queries the "book" edge of the BookReview entity.
func (_m *BookReview) QueryBook() *BookQuery {
	return NewBookReviewClient(_m.config).QueryBook(_m)
}

// QueryReviewer queries the "reviewer" edge of the BookReview entity.
func (_m *BookReview) QueryReviewer() *UserQuery {
	return NewBookReviewClient(_m.config).QueryReviewer(_m)
}

// Update returns a builder for updating this BookReview.
// Note that you need to call BookReview.Unwrap() before calling this method if this BookReview
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BookReview) Update() *BookReviewUpdateOne {
	return NewBookReviewClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BookReview entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BookReview) Unwrap() *BookReview {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BookReview is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BookReview) String() string {
	var builder strings.Builder
	builder.WriteString("BookReview(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("book_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.BookID))
	builder.WriteString(", ")
	builder.WriteString("reviewer_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ReviewerID))
	builder.WriteString(", ")
	builder.WriteString("action=")
	builder.WriteString(_m.Action)
	builder.WriteString(", ")
	builder.WriteString("status_from=")
	builder.WriteString(_m.StatusFrom)
	builder.WriteString(", ")
	builder.WriteString("status_to=")
	builder.WriteString(_m.StatusTo)
	builder.WriteString(", ")
	builder.WriteString("reason=")
	builder.WriteString(_m.Reason)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BookReviews is a parsable slice of BookReview.
type BookReviews []*BookReview

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/zhoumingjun/bookmgr/backend/ent/dimension"
)

// Dimension is the model entity for the Dimension schema.
type Dimension struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Slug holds the value of the "slug" field.
	Slug string `json:"slug,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int `json:"sort_order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DimensionQuery when eager-loading is set.
	Edges              DimensionEdges `json:"edges"`
	dimension_children *uuid.UUID
	selectValues       sql.SelectValues
}

// DimensionEdges holds the relations/edges for other nodes in the graph.
type DimensionEdges struct {
	// Parent holds the value of the parent edge.
	Parent *Dimension `json:"parent,omitempty"`
	// Children holds the value of the children edge.
	Children []*Dimension `json:"children,omitempty"`
	// BookDimensions holds the value of the book_dimensions edge.
	BookDimensions []*BookDimension `json:"book_dimensions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ParentOrErr returns the Parent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DimensionEdges) ParentOrErr() (*Dimension, error) {
	if e.Parent != nil {
		return e.Parent, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: dimension.Label}
	}
	return nil, &NotLoadedError{edge: "parent"}
}

// ChildrenOrErr returns the Children value or an error if the edge
// was not loaded in eager-loading.
func (e DimensionEdges) ChildrenOrErr() ([]*Dimension, error) {
	if e.loadedTypes[1] {
		return e.Children, nil
	}
	return nil, &NotLoadedError{edge: "children"}
}

// BookDimensionsOrErr returns the BookDimensions value or an error if the edge
// was not loaded in eager-loading.
func (e DimensionEdges) BookDimensionsOrErr() ([]*BookDimension, error) {
	if e.loadedTypes[2] {
		return e.BookDimensions, nil
	}
	return nil, &NotLoadedError{edge: "book_dimensions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Dimension) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dimension.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case dimension.FieldName, dimension.FieldSlug, dimension.FieldDescription:
			values[i] = new(sql.NullString)
		case dimension.FieldCreatedAt, dimension.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case dimension.FieldID:
			values[i] = new(uuid.UUID)
		case dimension.ForeignKeys[0]: // dimension_children
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Dimension fields.
func (_m *Dimension) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dimension.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case dimension.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case dimension.FieldSlug:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field slug", values[i])
			} else if value.Valid {
				_m.Slug = value.String
			}
		case dimension.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case dimension.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				_m.SortOrder = int(value.Int64)
			}
		case dimension.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case dimension.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case dimension.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field dimension_children", values[i])
			} else if value.Valid {
				_m.dimension_children = new(uuid.UUID)
				*_m.dimension_children = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Dimension.
// This includes values selected through modifiers, order, etc.
func (_m *Dimension) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryParent queries the "parent" edge of the Dimension entity.
func (_m *Dimension) QueryParent() *DimensionQuery {
	return NewDimensionClient(_m.config).QueryParent(_m)
}

// QueryChildren queries the "children" edge of the Dimension entity.
func (_m *Dimension) QueryChildren() *DimensionQuery {
	return NewDimensionClient(_m.config).QueryChildren(_m)
}

// QueryBookDimensions queries the "book_dimensions" edge of the Dimension entity.
func (_m *Dimension) QueryBookDimensions() *BookDimensionQuery {
	return NewDimensionClient(_m.config).QueryBookDimensions(_m)
}

// Update returns a builder for updating this Dimension.
// Note that you need to call Dimension.Unwrap() before calling this method if this Dimension
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Dimension) Update() *DimensionUpdateOne {
	return NewDimensionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Dimension entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Dimension) Unwrap() *Dimension {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Dimension is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Dimension) String() string {
	var builder strings.Builder
	builder.WriteString("Dimension(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("slug=")
	builder.WriteString(_m.Slug)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", _m.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Dimensions is a parsable slice of Dimension.
type Dimensions []*Dimension
